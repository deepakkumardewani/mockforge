---
title: Getting started
description: Call the hosted API in under a minute. No signup.
---

No API key. Send a request to the origin.

<DocsBaseUrl />

<DocsCurlList
  paths={["/api/products?limit=5", "/api/users/1", "/api/todos/search?q=grocery&limit=3"]}
/>

List responses use `{ data, total, limit, skip, meta }`. Writes return mock rows and do not persist.

Field shapes for each resource: [REST API](/docs/rest). Recovery key and quotas: [Identity & rate limits](/docs/identity).
